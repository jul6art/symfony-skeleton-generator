# Paquets installés dans TOUS les modes (composer require).
# Un paquet par ligne, # pour commenter.

symfony/monolog-bundle
symfony/lock                        # verrou des commandes planifiées + active la commande core:purge du core-bundle
jul6art/core-bundle                 # abstractions maison : contrôleur, manager, repository, voter, purge, événements, traits

# Suggestions :
# symfony/uid
# symfony/rate-limiter
