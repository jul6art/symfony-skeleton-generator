# Mode « api » : symfony new (skeleton nu) + stack API maison.
DESCRIPTION="API JSON (skeleton nu + Doctrine, Serializer, Validator, auth JWT, CORS)"
SYMFONY_NEW_FLAGS=""
NEXT_STEPS='    make db-create db-diff db-migrate            # schéma initial'
NEXT_STEPS_USER='    make user-create ARGS="moi@exemple.com --admin"  # premier compte
    make token EMAIL=moi@exemple.com PASSWORD=…  # récupère un JWT'
