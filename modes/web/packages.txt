# --webapp installe déjà : twig, asset-mapper, stimulus, ux-turbo, form,
# validator, security-bundle, doctrine + migrations, mailer, messenger,
# notifier, translation, serializer, http-client, intl, string…
#
# Ici : uniquement les ajouts maison.

symfonycasts/tailwind-bundle        # binaire Tailwind local, aucun build Node
symfonycasts/reset-password-bundle  # jetons de réinitialisation de mot de passe
symfony/rate-limiter                # login_throttling sur le formulaire de login

jul6art/auth-bundle                 # entité User, dépôt et manager

# Suggestions :
# symfony/ux-live-component
# symfony/ux-icons
# knplabs/knp-paginator-bundle
# symfonycasts/verify-email-bundle    # confirmation d'adresse à l'inscription
