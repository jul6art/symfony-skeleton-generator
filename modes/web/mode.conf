# Mode « web » : symfony new --webapp + Tailwind local + parcours de comptes complet.
DESCRIPTION="Application web complète (Twig + Tailwind local, inscription/connexion/mot de passe oublié, CRUD utilisateurs)"
SYMFONY_NEW_FLAGS="--webapp"
NEXT_STEPS='    make db-create db-diff db-migrate            # schéma initial'
NEXT_STEPS_USER='    make user-create ARGS="moi@exemple.com --admin"  # premier administrateur
    make tailwind-watch                          # recompile le CSS en continu'
