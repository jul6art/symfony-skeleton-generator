{% extends 'base.html.twig' %}

{% block title %}Modifier {{ user.email }} — {{PROJECT_TITLE}}{% endblock %}

{% block body %}
    <div class="mx-auto max-w-xl">
        <h1 class="mb-6 flex items-center gap-2 text-2xl font-semibold text-slate-900">
            <i class="fa-solid fa-pen text-indigo-600"></i> {{ user.email }}
        </h1>

        <div class="card">
            {{ include('admin/user/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
