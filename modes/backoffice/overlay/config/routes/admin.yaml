# L'écran de préférences d'apparence est livré par `admin-bundle`. La ROUTE, elle, appartient au
# projet : c'est ici que se décide son URL, donc aussi le pare-feu qui l'entoure.
admin_account_appearance:
    resource: '@AdminBundle/Controller/AppearanceController.php'
    type: attribute
    prefix: /admin
