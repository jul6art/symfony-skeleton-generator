/**
 * Ré-export du contrôleur `tooltip` de `jul6art/datatable-bundle`. Le chemin fixe l'identifiant
 * Stimulus `ui--tooltip`, celui que le balisage rendu par la datatable nomme.
 */
export { default } from '@jul6art/datatable-bundle/controllers/tooltip_controller';
