# Mode « api-platform » : symfony new --api (installe api-platform/api-pack).
DESCRIPTION="API Platform (REST + JSON-LD/Hydra, doc OpenAPI, GraphQL en option)"
SYMFONY_NEW_FLAGS="--api"
NEXT_STEPS='    make db-create                               # base de données
    make docs                                    # documentation interactive (/docs)
    make openapi                                 # export de la spec OpenAPI'
