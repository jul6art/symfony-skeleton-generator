# Mode « api-platform » : symfony new --api (installe api-platform/api-pack).
DESCRIPTION="API Platform (REST + JSON-LD/Hydra, doc OpenAPI, auth JWT, GraphQL en option)"
SYMFONY_NEW_FLAGS="--api"
NEXT_STEPS='    make db-create db-diff db-migrate            # schéma initial (entité User)
    make user-create ARGS="moi@exemple.com --admin"  # premier compte
    make docs                                    # documentation interactive (/api/docs)'
