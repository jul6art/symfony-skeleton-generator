twig:
    file_name_pattern: '*.twig'
    form_themes: ['form/theme.html.twig']
    globals:
        # Langues proposées par le sélecteur de la barre de navigation.
        enabled_locales: '%kernel.enabled_locales%'

when@test:
    twig:
        strict_variables: true
