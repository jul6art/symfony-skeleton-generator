{% extends 'base.html.twig' %}

{% block title %}{{ 'register.title'|trans }} — {{PROJECT_TITLE}}{% endblock %}

{% block body %}
    <div class="mx-auto max-w-md">
        <div class="card">
            <h1 class="mb-6 flex items-center gap-2 text-xl font-semibold text-slate-900">
                <i class="fa-solid fa-user-plus text-indigo-600"></i> {{ 'register.title'|trans }}
            </h1>

            {{ form_start(registrationForm) }}
                {{ form_row(registrationForm.email) }}
                {{ form_row(registrationForm.plainPassword.first) }}
                {{ form_row(registrationForm.plainPassword.second) }}
                {{ form_errors(registrationForm.plainPassword) }}

                <button type="submit" class="btn btn-primary w-full">
                    <i class="fa-solid fa-check"></i> {{ 'register.submit'|trans }}
                </button>
            {{ form_end(registrationForm) }}

            <p class="mt-6 text-sm text-slate-600">
                {{ 'register.already'|trans }} <a href="{{ path('app_login') }}" class="text-indigo-600 hover:underline">{{ 'login.submit'|trans }}</a>
            </p>
        </div>
    </div>
{% endblock %}
